# 二维几何约束、轨迹与变换验收

更新日期：2026-08-26  
范围：R5 数学第三批 `math.geometry.primitives-2d` / `geometrySpec`（`lesson-plan-1.3`）

## 已实现范围

- 构造点：中点、平移、旋转、轴对称、位似、垂足；
- 拖点约束：圆、无限直线、有限线段，拖动输入由本地运行时投影；
- 几何轨迹：只声明目标点、驱动参数和可选范围，固定本地采样 241 点；
- 构造依赖检查：拒绝缺失引用、自引用和间接循环；
- 轨迹对象可选择并单独调整颜色、线宽、实线/虚线/点划线和显示状态；
- 工作台与独立 HTML 使用相同语义，不依赖网络、模型或外部绘图库；
- MiniMax 首次生成、基于当前场景二次编辑、Skill 文件生成和管理员预审均识别新字段；
- 官方库与测试文件包含“旋转、圆周轨迹与垂足”。

## 自动验收

```bash
npm test
npm run build
node skills/word2html-scene-author/scripts/validate-package.mjs examples/lesson-packages/10-geometry-rotation-locus.word2html.json
```

单元与集成测试至少确认：全部六种构造结果、圆/直线/线段投影、241 点轨迹、旋转半径不变量、循环引用拒绝、LessonPlan 往返、场景包导入、官方库、管理员边界矩阵、对象样式和独立 HTML 运行时代码。

## 真实浏览器验收

先用带远程调试端口的 Chrome 打开当前开发页面，再运行：

```bash
npm run acceptance:geometry-transform -- 9333 http://127.0.0.1:5173
```

脚本检查官方旋转场景、公式位于画布上方、旋转角更新、241 点轨迹、轨迹显隐、轨迹对象虚线样式、圆约束拖动和离线 HTML。管理员还应人工检查窄屏、深色预设、最大/最小参数、标签遮挡和多个缩放级别。

2026-08-26 已在 Chrome 远程调试端口 `9333` 执行通过。实际确认了公式布局、旋转角更新、固定 241 点轨迹、轨迹显隐、对象级虚线、圆约束投影拖动、当前对象切换和离线 HTML 导出。窄屏、极值参数、标签遮挡和学科表述随后纳入 `word2html@0.1.0` 学科复核；所属二维几何能力现已标记为 `verified`。

## 当前边界

几何旋转仅构造点的位置，不模拟物体方向、角速度、转矩、惯量或碰撞；不支持符号求交、联动动画、多条关系曲线或任意脚本。上述内容继续由能力注册表返回具体缺口。
